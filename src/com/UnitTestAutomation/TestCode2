package com.UnitTestAutomation;
import java.awt.event.*;
import java.io.*;
import java.applet.*;
import java.util.*;
import java.math.*;
import com.UnitTestAutomation.ExternalClass1;
import com.UnitTestAutomation.ExternalClass2;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Lazy;
import org.springframework.stereotype.Component;

public class TestCode{

    @Autowired
    private RecordUtils recordUtils;

    @Autowired
    private RecordService recordService;

    @Autowired
    private LabelService labelService;

    public String Function1(ExternalClass1 obj1,ExternalClass2 obj2,int id) throws IndexOutOfBoundsException{
        String result=obj1.ExternalClass1Function1("hello");
        String result2=obj1.ExternalClass1Function2();
        obj2.ExternalClass2Function2();
        String recordInfo=recordUtils.getRecordInfoById(int id);
        String record=recordService.saveRecord(recordInfo);
        return record;
    }

    public String Function2(ExternalClass1 obj1) throws IOException{
        obj1.ExternalClass1Function3();
        String keyPrefix=recordUtils.getEntityKeyPrefix(String entityName);
        String label=labelService.getLabel();
        return "";
    }

    public int Function3(int a,int b) throws IndexOutOfBoundsException{

    }

    private void NonTestFunction() throws InstantiationError{
        return;
    }

    public static void main(String[] args){

        System.out.println("Hello");
    }

}